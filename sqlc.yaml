version: "2"
sql:
  - engine: "postgresql"
    queries: "query"
    schema: "migration"
    gen:
      go:
        package: "db"
        sql_package: "pgx/v5"
        out: "store/db"
        emit_json_tags: true
